"""백테스트 엔진 버전 (Single Source of Truth).

엔진은 최초 구현 이후 지속적으로 수정·업그레이드되어 왔다. 어떤 버전의 엔진으로
산출된 백테스트 결과인지 추적할 수 있도록, 모든 결과(BacktestResponse.version)에
아래 ``ENGINE_VERSION`` 값을 기록한다.

버저닝 규칙 (``MAJOR.MINOR.PATCH`` — 2026-08-18 개편, 사용자 지시)
-----------------------------------------------------------------
* **MAJOR** (``v16`` → ``v17``): 엔진의 계약·구조가 바뀌는 큰 전환. 저장된 전략의
  해석이나 결과 스키마가 달라져 **과거 결과를 같은 기준으로 볼 수 없는** 수준.
* **MINOR** (``v16.1`` → ``v16.2``): 백테스트 *결과값이 바뀌는* 변경 — 체결·사이징·
  리스크 청산·지표 계산·데이터 정합 수정, 그리고 새 지표·주기 신설(opt-in 가산).
* **PATCH** (``v16.1.0`` → ``v16.1.1``): 결과값이 **바뀌지 않는** 변경 — 청산 사유
  라벨·통계 표현·로깅·리팩터·표시 수준의 버그 수정.

개편 이유: 종전 2단계에서는 '결과값이 바뀌면 MAJOR'라 자릿수가 너무 빨리 올라갔다
(12.0 → 16.1이 3주). 결과 변경을 MINOR로 내리고 PATCH를 신설해, 앞자리는 엔진의
성격이 바뀔 때만 움직인다. **2026-08-18 이전 항목은 옛 2단계 표기**(MAJOR=결과값
변경)이며, 키만 세 자리로 맞춰 두었다(``16.1`` → ``16.1.0``) — 그 시절 앞자리 상승은
지금 기준으로는 대부분 MINOR에 해당한다.

버전을 올릴 때는 아래 ``CHANGELOG``에 한 줄을 추가하고 ``ENGINE_VERSION``을
갱신한다. (이 값이 유일한 기준이며, 다른 곳에 버전 문자열을 하드코딩하지 않는다.)

CI 가드(``scripts/check_engine_version_bump.sh`` — .github/workflows/ci.yml의
``engine-version-guard`` 잡)가 엔진 핵심 파일이 바뀌었는데 ``ENGINE_VERSION``이
그대로면 실패시켜 갱신을 상기시킨다. 결과 불변 리팩터면 커밋 메시지에
``[skip-version-bump]``.
"""

# ── 현재 엔진 버전 (유일 기준) ────────────────────────────────────────────────
ENGINE_VERSION = "16.33.0"

# 사람이 읽을 수 있는 한 줄 요약. 결과 배지/툴팁 등 표시용이며 값 계산엔 영향 없음.
ENGINE_VERSION_LABEL = "v16.33.0 — 결과 기준값 정비(무위험수익률·배당세·물가)와 정기 인출·비교 지수 선택·자산 상관/프론티어"

# ── 버전 이력 ─────────────────────────────────────────────────────────────────
# 과거 git 이력을 참고해 주요 변경 시점을 정리한 것. 정확한 커밋 단위 이력은
# git log를, 큰 흐름은 아래 표를 참고한다.
CHANGELOG = {
    "16.33.0": "**결과 기준값 정비 + 현금흐름·비교 확장(MINOR, 결과값 변경).** "
               "① **무위험수익률**: 샤프·소르티노·정보비율의 기준 금리를 0으로 두던 것을 백테스트 창의 실제 단기금리 "
               "평균(한국 CD 3개월 `kr3m`·미국 3개월물 `us3m`)으로 바꿨다 — 금리가 높던 구간의 위험조정 성과가 실제보다 "
               "좋게 나오던 문제. `options.risk_free_rate`를 주면 그 값이 이기고, 자료가 없으면 0으로 두되 결과의 "
               "`riskFreeRate`에 근거(series·source)를 싣는다. "
               "② **배당 원천징수세**: 배당 재투자(토탈리턴)를 세전 전액으로 굴리던 것을 세후로 바꿨다(한국 15.4%·미국 15% 기본, "
               "`options.dividend_tax_rate`로 덮어쓰기, 0이면 종전 동작). 가격 패널을 바꾸므로 Phase1 캐시 키 재료다. "
               "③ **물가 조정 수익률**: 창 기간 CPI(`kr_cpi` 세계은행 연간·`us_cpi` FRED 월간)로 실질 CAGR·실질 총수익률을 "
               "결과 `inflation`에 싣는다. 물가 자료가 창 끝보다 이르면 **ffill로 늘리지 않고** 닿은 구간과 `covered=false`를 그대로 공시한다. "
               "④ **정기 인출**: `risk.withdrawal_amount`·`withdrawal_period` — 각 주기 첫 거래일마다 현금 → 보유 평가액 비례 매도 "
               "순으로 마련해 내보낸다(매도 수수료·거래세 차감, 첫 봉 제외). 납입과 함께 쓰거나 인출만 요청할 수 있고(은퇴 인출 "
               "시뮬레이션), 보유를 다 팔아도 모자란 회차는 만들 수 있는 만큼만 인출하고 고지한다. 현금 풀 방식과는 함께 쓸 수 없다. "
               "⑤ **비교 지수 선택**: `options.benchmark`(kospi·kospi200·kosdaq·sp500·nasdaq100·dow·russell2000 또는 종목코드/티커) — "
               "유니버스 기본 벤치마크 대신 쓰고 다중 벤치마크 표에도 함께 싣는다. 읽지 못하면 조용히 기본값으로 떨어지지 않고 고지한다. "
               "⑥ **자산 상관·효율적 프론티어**: 결과 `analytics.portfolioMix` — 실제 보유했던 종목(최대 12개)의 상관행렬과 "
               "롱온리 효율적 프론티어(과거 통계). 가격이 굳은 종목(연 변동성 1% 미만)은 최소분산 해를 왜곡하므로 뺀다.",
    "16.32.0": "**유니버스 사전 필터 확장(MINOR, opt-in).** ① `risk.universe_exclude_loss_making`('net'=당기순손실·'operating'=영업손실·'both') — 그 시점에 알려진 최신 재무가 적자인 종목을 매 거래일 유니버스에서 뺀다(Phase1이 싣는 `net_income`·`ebit` 패널, 신호와 같은 지연). **재무를 모르는 종목은 빼지 않는다**(fail-open): 상장폐지 종목의 재무 커버리지가 1% 수준이라 모르면 제외로 처리하면 상폐 종목이 통째로 사라져 생존 편향이 들어온다 — 판정하지 못한 종목 수는 결과 경고로 알린다. ② `risk.universe_market_cap_exclude_bottom_pct` — 그날 횡단면 시가총액 하위 X% 제외(거래대금 하위 분위 제외와 같은 방식, 기준만 시가총액). 두 필터 모두 기존 사전 필터와 같은 자리·같은 마스크 경로다.",
    "16.31.0": "**매크로 조건 필터(MINOR, opt-in).** `risk.macro_filters` [{series, mode, operator, value, period, exposure_pct}] — "
               "금리·환율·VIX 등 시계열(`data/macro/<id>.parquet`, `engine/macro_data.MACRO_SERIES` 13종: vix·usdkrw·usdjpy·"
               "dxy·us10y·us2y·us3m·us_spread·fed_funds·kr10y·kr3m·gold·wti, 야후·FRED에서 야간 동기화 "
               "`backend/scripts/sync_macro_series.py`)이 조건을 충족하는 날 목표 노출을 exposure_pct로 줄인다. mode: level(수준)·"
               "change(period일 변화율 %)·ma(period일 이동평균 대비 위/아래). 시리즈 날짜에 판정해 거래일에 ffill, next_open은 "
               "하루 지연(국면 필터와 같은 규칙), 월간 시계열은 월말로 옮겨 다음 달부터 보인다. 여러 조건은 OR이며 가장 낮은 노출·"
               "그 사유. 시장 국면 노출과는 min으로 겹친다. 자료 없는 시리즈는 경고하고 건너뛴다. 아울러 `RiskManagement` 스키마에 "
               "`taa`(v16.29)가 빠져 HTTP 요청에서 전술 자산배분 설정이 버려지던 결함을 함께 고쳤다.",
    "16.30.0": "**결과 심화 분석·견고성 도구(MINOR, 부가 통계).** ① 결과 `analytics`(`engine/result_analytics.py`): 종목·섹터 "
               "손익 귀인(초기 자본 대비 %), 유니버스 내 팩터 회귀(MKT=벤치마크, SMB/HML/MOM=유니버스 상·하위 30% 월별 롱숏 — "
               "30종목·60관측 이상), 거래 수익률·보유일 히스토그램과 MAE/MFE(고가·저가 패널), 역사적 VaR/CVaR 95·99%, "
               "63일 롤링 샤프·롤링 베타, 회전율(기간·연환산, `turnover`로도 노출), 유동성(주문금액÷ADV20 참여율 분포·"
               "10% 참여율 기준 운용 가능 자본), 지역 표준 벤치마크 3종 대비 수익률·β·α·IR. 최적화 세션에서는 만들지 않는다. "
               "② 견고성 도구(`engine/robustness.py`, /rolling-start·/cost-sweep): 창 고정 롤링 시작일 반복·수수료×슬리피지 "
               "격자 반복 — 같은 엔진 결과의 요약 표. 체결·자산곡선·핵심 지표 불변.",
    "16.29.0": "**경쟁 격차 2차 — 캔들 패턴·다중 타임프레임·전술 자산배분(MINOR).** ① 캔들 패턴 조건 "
               "(`candle_pattern` + pattern 13종: 망치형·교수형·역망치형·유성형·도지·상승/하락 장악형·관통형·먹구름형·"
               "샛별형·저녁별형·적삼병·흑삼병 — `indicators.candle_pattern_mask`, 완성 봉에 신호, 망치·유성 계열은 "
               "직전 5봉 추세 문맥). ② 다중 타임프레임: 기술 조건의 `timeframe=weekly|monthly`는 주봉(금요일 라벨)·"
               "월봉(월말 라벨) OHLCV로 같은 지표를 계산해 일봉에 봉 종료일부터 ffill(`<열>__weekly`, 미완성 봉 미노출) — "
               "신호·사유는 '(주봉 기준)' 꼬리. ③ 전술 자산배분 템플릿(`ranking_metric='taa'`, `risk.taa` {model: vaa|daa|paa, "
               "offensive, defensive, canary, top_n}): `engine/taa.py`가 13612W 모멘텀(12·1M+4·3M+2·6M+12M)으로 매일 목표 "
               "비중 패널을 만들고, 랭킹 점수·후보(비중>0)·비중 방식 'schedule'(기준값 비례)로 순수 리밸런싱 경로에 태운다"
               "(월간 리밸런싱, VAA=공격 음수 ≥ breadth면 방어 최고 100%, DAA=카나리아 음수 비율만큼 방어, PAA=SMA252 "
               "대비 양수 폭으로 채권 비중 (N−n)/(N−a)). 새 조건·설정이 없는 전략은 결과·해시 비트 동일.",
    "16.28.0": "**경쟁 격차 1차 — 설정·엔진 12종 opt-in 가산(MINOR).** ① 비중 방식(`allocation_type`) 확장: "
               "market_cap(시총 비례)·min_variance·risk_parity(공분산 ERC — 종전 09-19의 역변동성 근사 폐지)·"
               "max_sharpe·min_cvar(Rockafellar–Uryasev LP)·fixed(`target_weights` 정적 배분), 산정 기간은 "
               "`allocation_lookback_days`(기본 60), 자료 부족·해 없음은 동일가중 폴백 + 횟수 고지"
               "(`engine/portfolio_weights.py`). ② 밴드 리밸런싱(`rebalance_threshold_pct`): 전일 종가 기준 드리프트가 "
               "밴드를 넘은 날 비중을 목표로 되돌린다(달력 주기 없으면 첫 거래일 편입 뒤 밴드만). ③ 최소 보유 기간"
               "(`min_holding_days`): 그 안에는 매도 신호·손절·익절·트레일링·편출 미적용. ④ 재진입 금지"
               "(`stop_cooldown_days`)·트레일링 활성화 임계(`trailing_stop_activation_pct`). ⑤ 지정가: 매수 "
               "`entry_limit_pct`(전일 종가 −x%, 저가 도달 시 min(시가, 지정가) 체결, 미체결은 다음 날 재판단), 매도 "
               "`exit_limit_pct`(+y%, 미체결은 다음 거래일 시장가; 리스크 청산은 시장가 유지). ⑥ 분할 매수"
               "(`entry_tranches` {count, step_pct} — 첫 회차 시장가, 이후 기준가 사다리)·분할 익절(`partial_take_profits` "
               "[{profit_pct, sell_pct}]). ⑦ 포지션 사이징(`position_sizing`): atr_risk(비중=위험%÷(배수×ATR÷주가), "
               "ATR 없으면 동일 비중)·kelly(완결 20건부터 승률·손익비, 켈리≤0이면 동일 비중). ⑧ 현금 대체 자산"
               "(`cash_asset`): 미투자 몫을 그 종목으로 보유(듀얼 모멘텀 안전자산) + 절대 모멘텀 임계"
               "(`absolute_momentum_threshold_pct`, 수익률 랭킹 후보 제외). ⑨ 익일 평균가 체결(`execution_timing="
               "next_avg`, (O+H+L+C)/4). ⑩ 거래량 비례 슬리피지(options `slippage_model=volume_impact`, "
               "기본+계수×√(주문금액÷ADV20), 1차 체결 규모로 산정 뒤 재실행). 새 설정이 없는 전략은 결과·해시 비트 동일.",
    "16.27.0": "**퀀트 표준 기능 4차 — 데이터 수집 확장(MINOR).** ① `ncav_ratio`(시가총액÷(유동자산−부채총계) %): DART "
               "재무상태표 유동자산·부채총계를 재무 캐시에 더하고(`parse_dart_ncav_inputs`, 야간 조회도 포함) parquet에 "
               "`ncav`(원)를 싣는다(`scripts/backfill_ncav.py`, 빈 칸 병합·프로덕션 실행). 비율은 런타임 계산, NCAV≤0은 "
               "NaN. ② 분기 손익 3항목(매출·영업이익·당기순이익, 3개월 원)을 분기 실적 수집에 추가(EPS와 같은 응답, 4분기="
               "연간−3분기 누적)하고 `{revenue,operating_income,net_income}_growth_{qoq,yoy}` 6종을 발표일 as-of로 런타임 "
               "계산(직전 분기 2~4개월·전년 동기 11~13개월 날짜 대조, 기준≤0은 값 없음). 수집 전 종목은 조건에서 빠진다.",
    "16.26.0": "**퀀트 표준 기능 3차 — opt-in 가산(MINOR).** ① 재무 지표 `f_score`(피오트로스키 0~9점): 연간 as-of "
               "시계열과 365일 전 값으로 9항목(ROA>0·영업CF>0·ΔROA>0·영업CF>순이익·Δ부채비율<0·Δ유동비율>0·신주 발행 "
               "없음(시총÷종가 근사, 1% 허용)·Δ매출총이익률>0·Δ자산회전율>0)을 세며 첫해·재료 결측은 NaN. 백필 없이 "
               "런타임 계산. ② 결과 `factorIc`: 랭킹+정기 리밸런싱 전략에서 리밸런싱일 점수 순위와 다음 리밸런싱일까지 "
               "종가 수익률 순위의 스피어만 IC(평균·표준편차·ICIR·양수 비율·상하위 5분위 수익률 차·기간 수). 체결·"
               "자산곡선·지표 불변(부가 통계).",
    "16.25.0": "**퀀트 표준 기능 2차 — opt-in 가산(MINOR).** ① 계절 필터(`risk.seasonal_months`): 행 날짜의 달이 "
               "목록에 없으면 노출 0(전량 현금, 사유 '계절 필터 청산'), 시장 국면 노출과 곱으로 중첩. ② 목표 변동성"
               "(`risk.target_volatility_pct`): 1회 실행 자산곡선의 최근 20거래일 변동성(연환산 246일)이 목표를 넘는 "
               "날은 노출을 목표÷실현 비율(5%p 단위 내림, 최대 100%·레버리지 없음)로 낮춰 재실행(t 종가 판정 → "
               "t+지연 적용). 낙폭 한도는 그 위에 덮인다. ③ 결과에 벤치마크 대비 베타·젠센 알파(연환산 %)·추적오차·"
               "정보비율(정의 불가는 null). ④ 재무 지표 `asset_growth`(총자산 as-of 시계열 1년 전 대비 %)·"
               "`accruals_ratio`((순이익−영업현금흐름)÷총자산 %) — 총자산=자본총계×(1+부채비율/100), 백필 없이 "
               "런타임 계산, 랭킹 전용 런타임 지표도 Phase1이 해결. ⑤ 반기(semiannual) 리밸런싱을 대화 레인이 받는다. "
               "새 설정이 없는 전략은 결과·해시 비트 동일(랭킹 지표 컬럼이 parquet에 있던 전략도 불변).",
    "16.24.0": "**퀀트 표준 기능 3종 — opt-in 가산(MINOR).** ① 포트폴리오 최대 낙폭 한도(`risk.max_mdd_limit_pct`)를 "
               "시뮬레이터가 집행한다 — 종전엔 스키마·프롬프트·요약 카드까지 있었지만 시뮬레이터가 읽지 않는 죽은 "
               "코드였다. 자산이 고점 대비 한도 이상 내려간 첫날의 다음 체결 시점(익일 시가=다음 봉, 당일 종가=그날)에 "
               "전량 현금화(사유 '포트폴리오 최대 낙폭 한도 도달'), 다음 리밸런싱일(정기 리밸런싱이 없으면 다음 "
               "거래일)부터 전략 규칙대로 재편입, 고점은 재편입 시점 자산에서 다시 잡는다. 구현은 자산곡선 감시→노출 0 "
               "덮어쓰기→재실행(한도에 닿은 횟수+1회). ② 복합 순위 합산의 지표별 가중치(`ranking_components[*].weight`, "
               "백분위 가중 평균 — 묶음 안에서만, 묶음끼리는 동일 가중). ③ 업종 제외 필터(`exclude_sectors` — "
               "섹터 제한 뒤 정본 분류로 뺀다, 미상 종목은 남김) + 묶음 통칭 '금융'=은행·증권·보험·금융지주. "
               "한도·가중치·제외가 없는 전략은 결과·해시 비트 동일.",
    "16.23.1": "**매매 기록 매수 행을 실체결 장부(pf.orders)에서 생성 — 표시 수리(PATCH).** 종전에는 "
               "청산 단위 기록(vectorbt exit trades) 1건마다 매수 행도 1개씩 지어냈다. 청산 기록은 부분 매도가 "
               "일어날 때마다 1건이고 진입일은 포지션이 처음 열린 날로 고정·진입가는 평균 매입가·수량은 그 부분 "
               "매도 수량이라, 리밸런싱으로 여러 번 잘라 판 한 포지션이 \"첫날에 여러 번 산 것\"처럼 보였다"
               "(2026-09-22 실측: JB금융지주 매수 13행 전부 2023-09-22, 가격 10,165→19,738 표류). 이제 매수 행은 "
               "매수 주문 1건당 1행으로 체결일·체결가·체결 수량을 그대로 쓰고, 매도 행은 종전대로 청산 기록에서 "
               "손익·사유를 붙인다(매수·매도 행 수가 더는 1:1이 아니다). 체결·자산곡선·지표 불변.",
    "16.23.0": "**정액 적립식 대상에 유니버스 허용 — opt-in 가산(MINOR).** 종전 적립 레인은 지정 종목"
               "(`backtest_mode='single_asset'`)만 받고 그 밖은 거절했다. \"S&P500 ETF를 100만 원씩\"처럼 "
               "**상품 유니버스**를 말한 요청은 국내 S&P500 ETF 전체가 대상이므로(2026-09-22 사용자 결정), "
               "유니버스 해석이 끝난 종목 전체에 회차 납입액을 균등 분할한다 — 지정 종목이 여럿일 때와 같은 "
               "장부이고, 1주 가격이 종목별 몫보다 비싼 회차는 종전대로 현금으로 남겨 다음 회차에 합쳐 산다. "
               "조건·랭킹·손절·익절과 섞인 요청을 거절하는 가드는 그대로다. 지정 종목 적립식은 경로·결과 "
               "모두 불변(거절되던 요청만 새로 계산된다).",
    "16.22.0": "**정액 적립식 현금 풀 — opt-in 가산(MINOR).** `risk.contribution_funding='cash_pool'`이면 "
               "밖에서 돈이 들어오지 않고 **초기 자본(보유 현금)에서 회차 매수액을 꺼낸다** — 초기 자본을 첫 봉에 "
               "전부 사지 않고 현금으로 들고, 각 주기의 첫 거래일(첫 봉 포함)마다 그 회차 매수액(기본액·조건부 "
               "규칙은 v16.21과 같은 산식)을 산다. 회차 매수액은 두 한도로 깎인다: `max_buy_cash_pct`(그 시점 보유 "
               "현금 대비 %)와 현금 하한(`cash_reserve_pct`=초기 자본 대비 % 또는 `cash_reserve_amount`=금액, 둘 중 "
               "하나). 한도에 걸리면 종목별 몫을 같은 비율로 줄이고 남은 돈은 풀에 남는다(종목별 예산 이월 없음). "
               "납입 흐름은 첫 봉의 초기 자본 하나뿐이라 시간가중 수익률이 일반 백테스트의 수익률과 같은 뜻이 되고, "
               "벤치마크도 목돈 1회 곡선이 된다. 결과 `contributions`에 `funding`·`investedTotal`·`finalCash`·"
               "`cashReserve`·`maxBuyCashPct`·`limitedRounds`를 싣는다. 표기 오류(하한 ≥ 초기 자본, 비율 범위 밖, "
               "하한을 %와 금액으로 동시에 지정)는 Fail Fast. 풀 요청이 아닌 적립식은 종전 경로 그대로다(결과 불변).",
    "16.21.0": "**정액 적립식 조건부 납입액 — opt-in 가산(MINOR).** `risk.contribution_rules`"
               "([{condition, amount, mode}])가 있으면 납입일마다 종목의 조건을 봐서 그 회차 납입액을 바꾼다 — "
               "기본액에서 시작해 `set` 규칙이 성립하면 그 금액으로(여럿이면 가장 큰 금액), `add` 규칙이 "
               "성립하면 더한다('200일선 아래면 200만 원', 'RSI 30 미만이면 100만 원 더'). 조건 평가는 일반 매수 "
               "신호와 같은 평가기(SignalEngine)이고 판정 시점도 같다 — 시가 체결(next_open)은 전 거래일 종가까지의 "
               "값, 종가 체결은 당일 종가(미래 참조 금지). 초기 자본(1회차)에는 적용하지 않고, 종목이 여럿이면 "
               "종목별 몫에 종목별로 적용한다. 납입액이 달라지므로 총 납입액·금액가중 수익률·적립 벤치마크도 같은 "
               "납입 일정을 따른다. 규칙별 적용 횟수는 `contributions.rules`로 싣는다. 규칙이 없는 요청은 종전 "
               "경로 그대로다(결과 불변).",
    "16.20.0": "**정액 적립식(DCA) 신설 — opt-in 가산(MINOR).** `risk.contribution_amount`(회차 납입액)·"
               "`risk.contribution_period`(weekly|monthly|bimonthly|quarterly|semiannual|yearly|daily)가 있으면 "
               "지정 종목(`backtest_mode=single_asset`)을 납입 일정대로 조건 없이 사 모으는 별도 장부"
               "(`engine/contributions.py`)로 계산한다 — 일반 체결 경로(시뮬레이터·vectorbt)는 거치지 않는다. "
               "첫 봉은 초기 자본, 이후 각 주기의 첫 거래일(리밸런싱 달력과 같은 규칙)마다 납입하고 그 봉의 "
               "체결가로 산다. 납입금은 종목별로 균등하게 나누고 정수 주만 사며 잔돈은 종목 예산에 이월된다"
               "(1주 값 > 예산이면 다음 회차에 합쳐 매수, 납입일에 거래 불가면 거래 가능해지는 첫 봉에 매수). "
               "자산곡선이 납입으로도 올라가므로 `totalReturn`·`cagr`·`maxDrawdown`·`sharpe` 등은 **시간가중 "
               "수익률**(r[t] = 평가액[t] ÷ (평가액[t-1] + 납입[t]) − 1)로 계산해 일반 백테스트와 같은 뜻을 "
               "유지하고, 총 납입액·평가 손익·단순 수익률·금액가중 수익률(XIRR)은 새 결과 키 `contributions`에 "
               "싣는다. 벤치마크 곡선도 같은 날 같은 금액을 넣은 곡선이다. 매도가 없어 거래 통계(승률·PF 등)는 "
               "계산하지 않는다. 매수·매도 조건, 랭킹, 손절·익절과 섞인 요청·유니버스 요청은 거절한다(Fail Fast). "
               "결과 처리기의 벤치마크 수익률·샤프/소르티노·낙폭 기간 계산을 공용 헬퍼로 추출했다(값 불변). "
               "납입 필드가 없는 전략의 결과·해시는 그대로다.",
    "16.19.0": "**실적 서프라이즈 시그널 랭킹·섹터별 비중 상한·유니버스 사전 필터 신설 — opt-in 가산(MINOR).** "
               "① 새 랭킹 지표 `pead`(`engine/earnings_factor.py`): 분기 EPS의 전년 동기 대비 차이를 직전 8개 "
               "분기 같은 차이의 표준편차로 나눈 SUE와, 실적 발표일 직전 거래일~발표 2거래일 후의 시장 대비 "
               "초과수익률을 각각 횡단면 1% 윈저라이즈 z-score로 표준화해 평균한다. 발표 자격 창"
               "(`ranking_entry_delay_days` 기본 2·`ranking_expiry_days` 기본 60 거래일) 밖은 NaN이라 '발표 후 "
               "N일이 지난 종목만 편입하고 M일이 지나면 제외'가 시그널 자체로 표현된다. 새 발표가 나오면 옛 "
               "분기 시그널은 그 날 끊긴다. 원재료는 DART 분기보고서에서 받은 분기 EPS·공시 접수일"
               "(`engine/quarterly_earnings.py`, `data/quarterly-earnings/`)이며 **2016년 이후**만 있다 — 분기 "
               "실적을 모르는 종목·구간은 NaN(fail-closed). ETF·미국 시장·복합 순위 합산은 오류+제거+안내. "
               "② `risk.max_sector_weight_pct`(%)가 있으면 같은 섹터 종목의 목표 비중 **합**을 그 값으로 맞춘다 "
               "— 넘는 섹터만 안에서 비례 축소(섹터 안 상대 비중 보존)하고 잘린 몫은 현금이다. 종목당 상한 "
               "뒤에 적용하며, 섹터를 모르는 종목과 구성원이 하나뿐인 섹터는 제약 대상이 아니다. "
               "③ 유니버스 사전 필터: `risk.universe_market_cap_top_n`(매 거래일 실측 시총 상위 N — 지수 구성 "
               "종목이 아니라 순위)과 `risk.universe_liquidity_exclude_bottom_pct`"
               "(+`universe_liquidity_lookback_days`, 기본 20 — 그 기간 평균 거래대금 하위 X% 제외). 둘 다 "
               "결과 경고로 무엇이 적용됐는지 알린다. 값이 없는 전략의 결과·해시는 그대로다.",
    "16.18.0": "**종목당 비중 상한 신설 — opt-in 가산(MINOR).** `risk.max_position_weight_pct`(%)가 있으면 편입·"
               "리밸런싱 시점의 종목별 목표 비중을 그 값으로 자른다(`simulator._weight_cap` — 순수 리밸런싱 경로의 "
               "목표 비중 행, 조건 루프 경로의 동일가중·역변동성 진입 비중·비중 리셋). 잘린 몫은 다른 종목에 "
               "재배분하지 않고 현금으로 남는다. 상한 × 최대 종목 수 < 100%면 결과 경고로 알린다. 보유 중 주가 "
               "상승으로 상한을 넘은 비중은 다음 리밸런싱의 비중 리셋이 되돌린다(상시 감시 아님). 값이 없는 "
               "전략의 결과·해시는 그대로다.",
    "16.17.0": "**잔차 반전 시그널 랭킹 신설 — opt-in 가산(MINOR).** 새 랭킹 지표 `residual_reversal`"
               "(`engine/residual_factor.py`): 종목 일간 수익률을 자기 상장 시장 지수 수익률 + 소속 섹터 평균 "
               "수익률(전체 상장 종목 동일가중, 자기 제외)에 L거래일 회귀한 잔차 중 최근 K거래일 합 ÷ 회귀 구간 "
               "잔차 표준편차 → 전략 유니버스 횡단면에서 상하위 1% 윈저라이즈 → z-score → 부호 반전. 개방 "
               "파라미터는 이산값만(`ranking_lookback_days` L∈{60,120,250}, `ranking_accumulation_days` "
               "K∈{3,5,10,20}, 기본 60/5), 나머지는 고정. 가격은 백테스트 옵션과 무관한 지표 전용 전처리"
               "(수정주가+배당 TR+기업행위 정제, 전체 이력)를 쓴다. 시장·섹터를 모르는 종목(미국·ETF)·회귀 "
               "구간 결측·공선은 NaN → 후보 배제(fail-closed). 기본 조합은 사전계산 캐시"
               "(`data/factor_cache`, 데이터 지문 일치 시에만 사용), 나머지는 온디맨드 — 두 경로의 값은 "
               "비트 단위로 같다. 이 지표를 쓰지 않는 전략의 결과는 그대로다. 한계: 섹터 소속은 현재 시점 분류.",
    "16.16.0": "**시장 국면 필터에 변동성 급등 판정 추가 — opt-in 가산(MINOR).** `market_regime.triggers`에 "
               "`volatility_spike`를 넣으면 지수 일간 수익률의 N일 표준편차(`volatility_period`, 없으면 20거래일)가 "
               "그 값의 직전 252거래일 평균의 `volatility_multiple`배 이상인 날을 약세일로 본다. 이동평균 판정"
               "(`below_ma`)과 함께 있으면 OR(둘 중 하나만 충족해도 목표 노출 축소), 단독 사용도 가능. 기준선이 "
               "아직 정의되지 않은 초기 구간은 전액 투자. 판정 지연·노출 재조정은 v16.14와 같다. 매매 사유·결과 "
               "경고는 판정 종류별 템플릿(이동평균/변동성/둘 중 하나). `triggers`가 없는 기존 요청은 종전과 비트 동일.",
    "16.15.0": "**재무 지표 2종 신설 — opt-in 가산(MINOR).** ① `fcf_yield`(FCF 수익률 %, 잉여현금흐름 raw ÷ "
               "일별 시가총액 억원×1e8, `recompute_fcf_yield` — PCR과 같은 정의 자리·시총 재구축에도 배선) "
               "② `dividend_streak_years`(연속 배당 연수, ex-date별 주당배당의 달력 연도 합이 양수인 연도가 "
               "직전 연도부터 끊기지 않고 이어진 수 — 진행 중인 올해 제외, `dividends.dividend_streak_years`). "
               "둘 다 parquet에 컬럼이 없어도 data_resolver가 기존 컬럼(fcf·market_cap·dividends)에서 런타임 "
               "계산하므로 백필 불필요. 조건 필터·랭킹 구성 지표로 쓸 수 있다. 쓰지 않는 전략의 결과는 불변.",
    "16.14.0": "**전문가형 퀀트 전략 요소 — opt-in 가산(MINOR).** ① 수익률 랭킹 최근 제외 기간"
               "(`ranking_skip_days`·구성 지표 `skip_days`) — 12-1 모멘텀 = t-252 → t-21 구간 수익률. "
               "② 복합 순위 묶음 점수(구성 지표 `group`) — 같은 묶음은 먼저 백분위 평균 후 다른 묶음·단독 지표와 "
               "동일 가중 평균(품질 지표 수가 많다고 품질 가중이 커지지 않게). ③ 변동성 역비중"
               "(`allocation_type='inverse_volatility'`, `allocation_lookback_days`) — 목표 비중 ∝ 1/σ(N일 연환산), "
               "σ 미정의 종목은 후보 제외, 정기 리밸런싱 필요(없으면 경고 후 동일 비중). ④ 시장 국면 필터"
               "(`market_regime` = 지수·이동평균 기간·약세 노출 %) — 지수 종가 < N일 SMA인 날 목표 노출을 줄이고 "
               "국면이 바뀐 날 보유 비중을 기준 비중 × 노출로 다시 맞춘다(0% = 전량 현금화 후 신규 편입 없음, "
               "판정은 신호와 같은 지연). ⑤ 재무 필터 `trading_value`의 평균 기간(`period`, 종전 20일 고정). "
               "⑥ 재무 지표 `roic`(영업이익×(1−유효세율) ÷ (자본총계+이자부부채−현금))·`fcf_margin`(FCF ÷ 매출액) — "
               "DART 원재료 백필 후 값이 생긴다. 새 필드를 쓰지 않는 전략의 결과는 불변(비트 동일 테스트).",
    "16.13.0": "**거래대금 배수 지표 신설 — opt-in 가산(MINOR).** 새 기술 지표 `trading_value_ratio`(당일 "
               "거래대금 ÷ 직전 N일 평균 거래대금, 거래대금=종가×거래량, 배수 임계와 부등호 비교)로 \"최근 "
               "거래대금이 30일 평균보다 높은\"류 조건을 정확히 표현한다 — 종전엔 거래량 급증(OBV 교차)이나 "
               "거래량 배수로 근사됐다. 이 지표를 쓰지 않는 전략의 결과는 불변.",
    "16.12.0": "**창 경계 워밍업(랭킹 lookback·next_open 첫날) + 손절 종목 대체 편입 — 결과값이 바뀐다(MINOR).** "
               "① 랭킹 패널(N거래일 수익률·시장 대비 초과수익률·변동성·복합 순위의 가격 지표·후보 우선순위)이 "
               "창으로 잘린 종가로 계산돼 창의 첫 lookback 거래일 동안 순위가 없었다 — 랭킹 회전 전략이 그동안 "
               "현금으로 앉아 있었다(2026-09-17 실측: AI 테마 60거래일 수익률 상위 5종목·월간, 2023-09-18 시작 → "
               "첫 매수 2024-01-02). phase1이 워밍업 포함 종가(`window_boundary_prep`)를 따로 싣고 엔진이 그 패널로 "
               "계산한 뒤 창 인덱스로 되돌린다. 창 종가 패널(`raw_price_df`)의 가용성·상장·상폐 의미는 그대로이고, "
               "관측이 lookback 미만인 신규 상장 종목은 여전히 NaN(v13.3). lookback이 기본 워밍업(400일)보다 길면 "
               "워밍업을 늘린다. 창 시작 절단이 없는 요청(FULL)은 불변. "
               "② 랭킹 회전(선정=진입) 커스텀 루프에서 손절로 청산된 종목이 그 기간 목표 집합에 남아 다음 거래일에 "
               "재매수됐다(실측: 아티스트컴퍼니 2024-01 손절→재매수 3회). 사용자 결정으로 손절 종목은 그 리밸런싱 "
               "기간 목표에서 빠지고, 빈자리는 그 리밸런싱일 랭킹의 다음 순위(보유·이미 빠진 종목 제외, 분위 그룹은 "
               "자기 구간 안)가 채운다 — 남은 후보가 없으면 다음 리밸런싱까지 현금. 대체 매수 사유는 "
               "'손절 종목 대체 편입 (리밸런싱일 순위 N위)'(`STOP_LOSS_REFILL`, 시뮬레이터 `entry_reason_overrides`). "
               "익절·트레일링·보유 기간 만료·매수 조건 전략은 종전 동작(미결정). "
               "③ next_open(`execution_delay_days` N)의 N일 shift가 창 안에서만 일어나 창 첫 N거래일(첫날 = 첫 "
               "리밸런싱일)의 매수 신호·순위·유동성·시총 마스크가 늘 비었다 — 랭킹 회전은 첫 주기 전체가 현금, 매수 조건 "
               "전략도 첫날 체결 불가(실측: 2023-09-18 시작 → 첫 매수 2023-10-04). 사용자 결정으로 phase1이 창 직전 N+1봉"
               "(`window_boundary_prep`)을 싣고, 신호·유동성은 그 봉을 이어 붙인 프레임에서 계산하며(`symbol_signals`), "
               "엔진은 창 직전 N거래일을 앞에 붙여 민 뒤 창만 남긴다(매수·매도 신호, 수익률·변동성·초과수익률·재무·복합 순위, "
               "후보 우선순위, 유동성·시총 마스크). 창 첫날 결정은 창 직전 거래일 정보만 쓴다(창 중간 리밸런싱일과 같은 "
               "규칙, 룩어헤드 없음). 부수 효과: 창 첫 봉의 교차 신호와 전일 거래대금 유동성도 창 직전 봉을 보게 돼 "
               "same_close에서도 창 첫날 값이 바뀔 수 있다(창 둘째 봉 이후 신호는 불변 — 실데이터 850건 대조). "
               "창 이전 봉이 없는 종목(창 안 신규 상장)은 원천이 없어 첫날 선정 불가, 상폐·데이터 종료 강제청산과 "
               "raw_price_df 가용성은 불변. 세션 캐시 키에 signal_delay 추가. AI 하락 랭킹 청산(rank_exits)만 창 안 shift 유지. "
               "④ ③으로 창 첫날 시가 체결이 생기자 자산곡선 첫 값(첫 거래일 종가 평가액)이 초기자본과 달라졌는데, "
               "결과에 초기자본이 없어 프론트 매퍼 4곳이 `initialCapital = equity[0]`로 채웠다 — 로그 초기자금 "
               "9,748,557원·카드 총수익·ROI(+42.11%, 실제 +38.54%)·월별 첫 달이 첫날 손익을 뺀 값으로 나갔다(첫날이 "
               "늘 현금이던 종전엔 가려져 있었음). 결과에 `initialCapital`을 동봉하고(스키마 선언) 매퍼 4곳이 그 값을 "
               "쓴다. 엔진 안에서도 초기자본을 기준점으로 맞췄다: 최대낙폭(vbt max_drawdown은 곡선만 봐 첫날 낙폭 누락)·"
               "최장 낙폭 기간·회복계수(`ResultHandler.anchored_equity`/`max_drawdown_pct`), 분위 그룹·리밸런싱 기간 "
               "비교표의 낙폭·샤프(첫날 수익률 포함), 워크포워드 OOS 연결 곡선(창 초기자본 기준), 몬테카를로(백엔드·"
               "프론트, 첫 값≠초기자본일 때 첫날 수익률 포함), 벤치마크 첫날 수익률(창 직전 종가 대비 — 전략과 같은 "
               "출발점, 모든 창 지정 백테스트의 벤치마크 수익률이 첫날만큼 달라진다). 총수익률·총손익·CAGR·샤프는 "
               "원래 vbt가 초기자본 기준이라 불변.",
    "16.11.1": "**표시·성능 경로 수정 — 결과값 불변(PATCH).** ① 매매 사유의 연산자 낱말이 엄격 비교(`>`·`<`)도 '이상/이하'로 적어 `PER > 0` 전략의 사유가 \"PER 0 이상\"으로 찍혔다 — `>`='초과', `<`='미만'으로 평가 경로와 맞췄다(시장 대비 초과수익률 기본 연산자 라벨 포함). ② 워크포워드 창 병렬에서 세션 캐시 예산·Phase1 스레드를 띄운 워커 수(슬라이더 경로 힌트=MAX_WINDOWS→8)로 나눠, 창이 3개여도 1/8씩 쪼개졌다 — 실제로 도는 창 수로 나눈다(`WindowPool.window_resources`). 진행 이벤트 `workers`도 창 수를 넘지 않는다. 2026-09-17 코스피 PER·PBR 10년 워크포워드 실측.",
    "16.11.0": "**시점 유니버스 마스터 신선도 복구 — 결과값이 바뀐다(MINOR).** 시점 유니버스의 진실 소스 "
               "`data/stock-master.json`이 2026-06-13 생성본에서 멈춰 있어 ① 그 뒤 상장폐지된 24종목이 "
               "유니버스에서 통째로 빠지고(생존 편향) ② 마스터의 가격 커버리지 끝보다 뒤에서 시작하는 창은 "
               "as-of 해석이 0종목이 돼 엔진이 프론트가 보낸 **현재 상장 종목** 목록으로 조용히 폴백했다. "
               "마스터를 2026-09-15 기준으로 갱신(상폐 474→498)해 유니버스 구성이 달라지므로 종전 버전의 "
               "저장 결과와 값이 다르다 — 캐시 재사용도 이 경계에서 끊긴다. 갱신은 제자리 병합 "
               "(`backend/scripts/refresh_stock_master.py`, 섹터 후처리 보존·멱등)이며 일일 스케줄러에 배선했다. "
               "as-of가 비면 결과 경고(`PIT_UNIVERSE_MASTER_STALE`)로 생존 편향 가능성을 고지한다. SRS FR-VM-067b",
    "16.10.0": "**거래량 배수 지표·시장 대비 초과수익률 랭킹 신설 — opt-in 가산(MINOR).** ① 새 기술 지표 "
               "`volume_ratio`(당일 거래량 ÷ 직전 N일 평균 거래량, 배수 임계와 부등호 비교)로 \"거래량이 "
               "20일 평균보다 1.5배 많은\"류 조건을 정확히 표현한다 — 종전엔 OBV 교차(volume_spike)로 "
               "근사됐다. ② 새 랭킹 지표 `relative_return`(종목 N거래일 수익률 − 자기 상장 시장 지수 "
               "N거래일 수익률 순위)로 \"시장 대비 수익률 상위 N종목\"을 선정한다 — 코스피·코스닥 혼합 "
               "유니버스에서도 종목마다 제 시장 지수(data/index)를 빼므로 정확하고, 복합 순위 합산의 "
               "구성 지표로도 쓸 수 있다. 지수가 없는 종목은 NaN → 후보 배제. 두 지표를 쓰지 않는 전략의 "
               "결과는 불변.",
    "16.9.0": "**신호 후 N거래일 지연 체결 신설 — opt-in 가산(MINOR).** 새 옵션 `execution_delay_days`(risk/"
              "options, 기본 1)가 next_open 분기의 shift 폭이 된다: 신호·랭킹·유동성·시총 마스크를 N일 밀고 "
              "상폐·데이터 종료 강제청산 신호도 N봉 앞에 둔다(phase1). 리스크 청산(손절·익절·트레일링)은 "
              "보호 주문이라 종전대로 감지 다음 거래일 시가 체결. same_close에 1 초과 값은 거절. 기본값 결과 불변.",
    "16.8.0": "**체결 시점 별칭 `current_close`를 `same_close`로 정규화 — 해당 전략 결과값 변경(MINOR).** "
              "대화 레인('당일 종가 체결')이 내는 `current_close`가 엔진의 same_close/next_open 어느 분기에도 "
              "걸리지 않아 신호 shift 없이 **당일 시가**에 체결되고(종가보다 앞선 룩어헤드) 경고도 빠졌다. "
              "backtest_engine 옵션 입구 한 곳에서 정규화해 phase1·시뮬레이터·경고가 같은 값을 본다. "
              "기본 경로(next_open)·명시 same_close 결과는 그대로다.",
    "16.7.1": "**시장 대비 초과수익률 매매사유에 실측값 동봉 — 체결·지표 불변(PATCH).** 사유가 조건 기준값만 "
              "적어 \"시장 대비 초과수익률(63일) 0%p 이상\"으로 나오면 얼마나 앞섰는지 읽을 수 없었다. "
              "그 봉의 실측 초과수익률로 바꿔 싣는다(\"… +4.2%p\", 기준값 괄호는 사용자 지시로 생략, 템플릿 "
              "RELATIVE_RETURN_MEASURED). signals.get_condition_segments가 선택적 실측값을 받고 벡터·행별 "
              "경로가 relative_return 조건에만 행별 값을 꽂는다 — 다른 조건의 사유와 모든 결과값은 그대로다.",
    "16.7.0": "**시장 대비 초과수익률 지표 신설 — opt-in 가산(MINOR).** 새 기술 지표 `relative_return`"
              "(종목 N거래일 수익률 − 상장 시장 지수 N거래일 수익률, %p)로 \"최근 3개월 동안 시장보다 "
              "덜 떨어진 종목\"류 조건을 정확히 표현한다. 지수 시계열은 새 저장소 data/index/"
              "{KOSPI,KOSDAQ}.parquet(토스 Open API 2014-07~ 정본 + KIS 1996~2014-06 보충, "
              "scripts/backfill_index_history.py, 야간 갱신은 sync_data.py)이고, phase1이 종목의 "
              "상장 시장 지수 종가를 날짜 조인(engine/market_index.attach_index_close)한 뒤 "
              "지표 엔진이 계산한다. 지수 파일이 없거나 시장을 모르면(미국 종목) NaN → 조건 "
              "fail-closed. 기존 지표·체결 경로는 건드리지 않아 이 지표를 쓰지 않는 전략의 결과는 "
              "불변이다(qa_backtest_equivalence --wfa 불일치 0).",
    "16.6.0": "**단순이동평균 지속 상태 필터 신설 — opt-in 가산(MINOR).** ma_crossover에 EMA와 같은 `mode`(above/below)를 붙여 \"종가가 60일선 위에 있는 동안\"·\"20일선이 60일선 위 유지\"를 교차 이벤트가 아니라 매 봉 참인 게이트로 평가한다(signals `_eval_vec`·`_eval_row`·사유 세그먼트). 종전에는 SMA에 이 모드가 없어 상태 표현이 crosses_above로 옮겨져 조건이 교차 당일 하루로 좁아졌다 — 인터프리터 프롬프트 5-3도 상태는 부등호로 내도록 함께 고쳤다. mode 없는 기존 신호의 평가는 그대로다(결과 불변). 사유 템플릿 4개 추가(MA_STAY_ABOVE/BELOW·MA_PRICE_STAY_ABOVE/BELOW).",
    "16.5.1": "**결과 경고 구조화 — 체결·지표 불변(PATCH).** 결과 로그의 경고(warnings)는 엔진이 값을 박아 만든 "
              "완성 한국어 문장이라 /us에서 번역할 수 없었다(사전 키가 값마다 달라짐). 매매사유(16.4.2)와 같은 "
              "세그먼트 계약으로 옮겨 `warnings`(한국어, 종전과 동일)와 `warningParts`(정본 템플릿+인자)를 같은 "
              "순서로 싣는다 — 정본 engine/result_warnings.py, 생성 지점 backtest_engine·phase1·data_coverage. "
              "경고 순서는 한국어 문장 기준 정렬(종전 set 순서 → 결정적).",
    "16.5.0": "**편향 감사 수리 1차 — 결과값 변경(MINOR).** ① 거래량 0(정지)으로 데이터가 끝나는 상폐 종목의 "
            "강제청산이 거래정지 마스크에 지워져 백테스트 끝까지 동결가로 남던 결함 → 마지막 실봉에서 정산 "
            "(backtest_engine `_forced_exit`, 상폐 474개 중 168개 해당). ② 미국 종목엔 한국 ±30% 가격제한 전제의 "
            "코퍼레이트 액션 역보정(loader._sanitize_corporate_actions)을 적용하지 않는다 — 실제 갭을 미래 봉 "
            "비율로 과거에서 지우던 룩어헤드 제거(phase1·벤치마크 `sanitize_corporate_actions=not is_us_symbol`). "
            "③ 증권거래세를 전 기간 0.15% 고정 대신 매도 봉 날짜의 시행일 기준 법정 세율(engine/transaction_tax.py, "
            "0.30%→0.15%, 농특세 포함)로 적용 — 명시(0 포함)하면 고정. ④ 리밸런싱 비중 리셋 트림(양수 목표비중 셀의 "
            "실현 매도)에 매도 비용을 물린다(Simulator._run_orders — 1차 체결 주문 기록으로 셀을 찾아 재실행). "
            "⑤ KOSPI200·KOSDAQ150 상위 N 판정을 정적 주식수 × 주가 대신 파케이 일별 실측 market_cap(억원)으로, "
            "결측 셀만 근사. 가드·고지: 비용 음수 거절, 수수료+슬리피지 0 고지, 미국 지정 종목·테마 경로 생존편향 "
            "고지, KR 상폐 이력 하한 이전 구간 고지, AI 검증 구간 겹침 고지, Optuna n_trials 상한.",
    "16.4.2": "**매매사유 구조화 + US 체결가 소수 유지 — 체결·지표 불변.** ① 거래 내역의 매매사유를 완성 문장 "
            "대신 한국어 정본 템플릿+인자 세그먼트로 만들어(engine/trade_reason.py — 생성: signals·result_handler·"
            "simulator·phase1·backtest_engine 랭킹 사유) `condition`(종전과 같은 한국어 문장)과 `conditionParts`"
            "(표시 번역용 구조)를 함께 싣는다 — /us에서 사유가 한국어로 나오고 손익 꼬리표가 원화로 찍히던 문제의 "
            "근본 수정. 한국어 문장은 바이트 동일, 파츠는 응답 필드 가산. ② US 심볼의 signals price/amount가 "
            "원화식 정수 반올림으로 센트를 잃던 것을 심볼 단위 판별로 소수 유지(KR=정수 원 규약 그대로) — "
            "체결 계산은 원래 float라 거래·자산곡선·지표 모두 불변. 표시 수준 수정이라 PATCH. "
            "동일성 게이트 31/31 통과(qa_backtest_equivalence --wfa).",
    "16.4.1": "**'X 관련주' 회사 앵커 유니버스 해석 — 결과값 불변**(FR 미국 레인). engine/universe_pit.py에 "
            "`has_group_suffix`(범주 접미 표기 판정)와 `resolve_us_company_related`(미국 상장사 앵커 → 관계 기업 티커) "
            "위임 함수를 덧붙였다. 두 함수 모두 engine/us_knowledge_graph.py로 넘기는 껍데기이고, 호출부는 "
            "strategy_conversation의 자연어 해석·유니버스 확정·역량 검증 계층뿐이다 — 체결·사이징·지표 계산 경로는 "
            "한 줄도 바뀌지 않았다. 기존 전략을 재실행하면 거래·자산곡선·지표가 모두 동일하고, 종전에 해석되지 않던 "
            "표현이 새로 유니버스로 확정될 뿐이라 PATCH.",
    "16.4.0": "**리밸런싱 방식 선택 신설(FR-BT-067, 2026-08-26 사용자 지시).** 리밸런싱에는 종목을 바꾸는 방식만 "
            "있는 것이 아니라 **같은 종목의 비중을 원래 비율로 되돌리는 방식**도 있다 — 오른 종목은 오른 만큼 팔고 "
            "내린 종목은 내린 만큼 더 사서 균등 비중을 유지한다. `rebalance_method`로 고른다: "
            "`reconstitute`(기본, 종전 동작 = 리밸런싱일마다 목표 종목 재선정)와 `weights_only`(종목 교체 없이 "
            "비중만 균등 리셋, 목표 종목 수에 미달하는 빈 자리만 채움). 두 시뮬레이터 경로(순수 from_orders 목표비중·"
            "커스텀 루프)에 모두 구현했고, 커스텀 루프는 종전에 비중 리셋 자체가 없었으므로 이 방식에서만 "
            "리밸런싱일에 보유 전체 목표비중을 다시 준다. 방식을 말하지 않은 기존 전략은 `reconstitute`라 "
            "**결과값 불변**이며(전략 해시도 불변), 새 방식은 opt-in 가산이라 MINOR. "
            "함께 정리한 것: 순수 경로의 **비중 리셋 트림 청산 사유**(`리밸런싱 비중 조정`) — 종목 교체도 "
            "리밸런싱일마다 비중을 리셋하는데 그 부분 매도에 사유가 없어, 매도 조건을 하나도 말하지 않은 "
            "전략의 거래 내역에 '전략 매도 조건 충족'이 찍히고 있었다(실측). 라벨만 바뀌고 체결·지표는 불변.",
    "16.3.3": "**성능 — 결과값 불변 리팩터**(FR-BT-049c). ① 최적화·워크포워드 세션 동안 종목별 Phase1 산출물"
            "(지표·전처리)을 (종목·날짜 경계·배당·구조 파라미터 서명) 키로 재사용(engine/prep_cache.py), 세션 안에서는 "
            "결과 화면 전용 리밸런싱 6주기 비교를 만들지 않는다. ② 종목별 파이프라인을 순수 함수로 분리하고"
            "(engine/phase1.py) 상주 프로세스 풀에 결정적 샤딩(engine/phase1_pool.py) — Phase1은 GIL에 묶여 스레드로는 "
            "코어를 못 쓴다(실측 1스레드≈14스레드). ③ 워크포워드 창 단위 프로세스 병렬(engine/wfa_workers.py). "
            "④ 리밸런싱 비교는 시뮬레이션 직후 워커에 선제출해 결과 정리와 겹치고, 주기가 결과에 영향을 못 주는 전략"
            "(보유 상한 없음)은 1회만 계산해 6행으로 복제한다. 계산식·체결·지표는 한 줄도 바뀌지 않았고, 캐시/병렬 "
            "유무의 결과 동일성을 실데이터 28전략×4경로 게이트(scripts/qa_backtest_equivalence.py)와 유닛 테스트가 "
            "매번 대조한다 — 결과값 불변이라 PATCH.",
    "16.3.2": "거래 기록에 **비용 차감 후 순손익**(`pnl`)을 함께 싣는다(FR-BT-050 몬테카를로 감사 후속). 종전 거래 재표본은 `price×quantity` 차액을 손익으로 써 수수료·거래세가 빠진 총손익 기준이었고, 그래서 백테스트 성과와 기준이 어긋났다. 백테스트 지표는 불변이고 응답 필드가 추가될 뿐이라 PATCH(16.3.1과 같은 자리).",
    "16.3.1": "리밸런싱 기간별 결과 비교(FR-BT-064)를 백테스트 결과에 동봉 — 메인 시뮬레이션 뒤 같은 입력으로 "
            "`rebalancing_period`만 매일·매주·매월·분기·반기·연간으로 바꿔 시뮬레이션을 6번 반복하고 주기별 CAGR·MDD·샤프·"
            "손익비·거래 수·회전율을 `rebalanceComparison`으로 싣는다(분위 그룹 비교와 같은 구조, engine/rebalance_comparison.py). "
            "메인 결과값 불변 — 응답 필드 추가라 PATCH. 종전의 별도 SSE 실행·LLM 서술 방식(같은 날 도입)은 사용자 지시로 폐기.",
    "16.3.0": "**매수 조건이 있는 전략의 리밸런싱·후보 우선순위 재정의(2026-08-18 사용자 결정).** ① 후보는 매수 "
            "조건이 정한다: 신호·재무 필터 전략(entry.conditions 있음)은 리밸런싱을 켜도 커스텀 루프로 돌아 "
            "리밸런싱일이 아닌 날의 매수 신호도 빈 자리만큼 담고 매도 신호도 그대로 청산한다(종전엔 순수 랭킹 "
            "회전 경로와 같아 리밸런싱일 사이의 매수 신호가 전부 버려졌고 SL/TP가 없으면 매도 신호까지 무시됐다). "
            "리밸런싱일에는 그날 조건 충족 종목 중에서 다시 구성하고(미충족 보유는 편출), 비중 리셋은 없다(경고 고지). "
            "순수 랭킹 전략(선정=진입)은 종전 달력 회전 그대로. ② 랭킹을 말하지 않은 전략에서 매수 조건 충족 종목이 "
            "빈 자리(최대 보유)보다 많은 날 — 리밸런싱일 포함 — 은 **최근 60거래일 수익률이 높은 순**으로 우선 담고, "
            "실제로 넘친 날이 있었으면 경고로 고지한다. 종전의 저PBR·고ROE 블렌드 폴백(사용자가 말한 적 없는 선정 "
            "기준, 조용히 적용)은 폐지. 결과가 바뀌는 전략: 리밸런싱을 켠 매수 조건 전략 전부, 후보가 자리를 "
            "넘치던 날이 있는 전략.",
    "16.2.0": "수익률(모멘텀) 랭킹의 방향 존중 — `ranking_metric='return'`에 `ranking_direction='bottom'`(\"최근 3개월 "
            "수익률 오름차순\" 같은 역발상 선정)이 오면 백분위를 뒤집어 **수익률 낮은 순**으로 선정한다. 종전에는 이 "
            "분기만 direction을 읽지 않아(변동성·재무·복합 분기는 읽음) bottom 요청이 조용히 top(모멘텀)으로 실행됐고 "
            "매수 사유도 '상위'로 찍혀 결과 화면에서 알아볼 수 없었다. 방향 미지정·top은 결과 불변. 매수 사유는 방향에 "
            "따라 '수익률 상위/하위 N%'.",
    "16.1.0": "반기(`semiannual`) 리밸런싱 주기 신설 — 리밸런싱 기간별 결과 비교 분석(FR-BT-064)이 매일·매주·매월·"
            "분기·반기·연간 6주기를 같은 전략으로 재실행하기 위해 추가. 반기의 첫 거래일(1월·7월)에 리밸런싱한다. "
            "opt-in 가산 — 기존 주기(none/daily/weekly/monthly/bimonthly/quarterly/yearly) 결과 불변.",
    "16.0.0": "**두 EMA '정배열 지속 상태' 신설 + 가격 vs EMA 조건의 기간 전달.** ① 두 EMA의 상하 관계를 "
            "엔진이 **지속 상태**로 평가할 수 있게 됐다(`ema` + shortPeriod/longPeriod + mode=above/below → "
            "빠른 EMA가 느린 EMA 위/아래에 있는 동안 매 봉 참). 종전에는 두 기간을 주면 교차 이벤트뿐이라 "
            "\"20일 EMA가 60일 EMA 위에 **있는**\"을 표현할 방법이 없었고, 상태로 옮기려면 한 선을 버려 "
            "'가격 vs 60일 EMA'가 돼야 했다 — 진입 빈도가 '정배열 내내'에서 '교차 당일 하루'로 좁아지거나 "
            "사용자가 말한 선이 사라졌다. ② 선이 하나인 EMA 조건의 기간 전달: 컴파일러는 "
            "그 기간을 `long_period` 칸에 담는데 엔진 변환기(`strategy_converter`)는 `period`만 읽어, "
            "\"가격이 60일 EMA 위\" 조건이 기간 기본값 20으로 백테스트됐다(사용자가 말한 기간과 무관하게 "
            "항상 20일). 변환기가 `period → long_period → short_period` 순으로 읽도록 고쳐 사용자가 말한 "
            "기간이 그대로 전달된다. 함께(결과 영향 있음, 같은 계열의 조용한 왜곡): 두 EMA의 기간이 모두 "
            "주어지면 컴파일러가 `short_period`를 버리지 않는다(\"20일 EMA가 60일 EMA 위\"가 \"가격이 60일 "
            "EMA 위\"로 바뀌던 문제), 한 선만 주어졌을 때 registry 기본값으로 두 번째 선을 만들지 않는다"
            "(\"20일 EMA 이탈\"→20/60 교차·20/20 자기교차). 발견 경로는 예시 QA 값 대조(FR-STR-019x ③).",
    "15.0.0":"**재무 필터·랭킹을 쓰는 전략의 결과가 바뀐다 — 연간 재무 forward-fill 상한 15개월.** 연간 "
            "레코드 하나가 공개일부터 다음 레코드까지 **무제한** 이어져, 레코드가 빈 연도(자리표시자 제거·"
            "미제출·상폐 후·DART 결손)에 직전 값이 그대로 남았다(부국증권 실측: 2013년 값이 2024년까지 "
            "10년, 활성 208종목·20.9만 종목-일). 이제 레코드는 공개일부터 15개월(결산 12 + 제출 지연 3)만 "
            "이어지고 그 뒤는 키별로 '데이터 없음'이 되어 필터가 fail-closed로 제외한다(매년 정상 보고하는 "
            "회사는 다음 레코드가 그 안에 와서 불변). rebuild_fundamental_columns는 캐시 지배 구간(가장 이른 "
            "공개일 이후)에서 NaN까지 캐시가 통째로 이기도록 바뀌어 stale 값을 회수한다(전 종목 재구축). "
            "함께: KIS 토큰 실패를 'KIS에 없음'과 구분(kis_pending 캐시, 부정 캐시 금지), 미러 rsync "
            "stall 재시도(300초·5회) — 둘은 결과값 불변.",
    "14.1.0": "PSR 폴백(시가총액 ÷ 매출액) — 14.0이 걷어낸 KIS 자리표시자 회사는 SPS가 없어 PSR이 영구 공백으로 "
            "남았다(활성 약 250종목). 같은 DART 손익계산서 응답의 매출액(ifrs-full_Revenue, 추가 호출 0)을 "
            "revenue(억원)로 저장하고, psr이 비어 있는 날만 일별 실측 market_cap ÷ revenue로 채운다"
            "(fill_psr_from_market_cap — enrich·rebuild 단일 정의, PCR과 같은 구조라 주식수 근사 왜곡 없음). "
            "**폴백 전용**: KIS SPS가 있는 날의 psr(종가÷SPS)은 불변이라 기존 PSR 결과는 바뀌지 않고 공백만 "
            "메운다. 14.0과 같은 날 신설·미배포라 MAJOR 대신 패치로 남긴다(13.2 선례). DART가 없는 구간"
            "(2015년 이전·매출 계정 없는 제출본)은 여전히 공백.",
    "14.0.0": "**재무 필터(PER·PBR·ROE·부채비율 등)를 쓰는 전략의 결과가 바뀐다 — KIS 0 자리표시자 "
            "연도 제거.** KIS 재무 엔드포인트는 재무를 싣지 않은 회사·연도에도 행을 돌려주되 값을 "
            "전부 0으로 채운다(삼진제약 실측: 22개 연도 전부 eps 0·sps 0·bps 0·부채비율 0). 연결재무제표를 "
            "만들지 않는 회사와 KIS 이력 시작 전 연도(2004~2009 다수)가 이렇고, 0을 진짜 값으로 받아 "
            "forward-fill한 결과 활성 124종목(동국제강·케이카·크라운제과·삼진제약·현대약품 …)이 최근 1년 "
            "내내 PER·PBR·PSR·ROE null로 가치 필터에서 통째로 사라졌고, 부채비율 0이 '부채비율 ≤ N' "
            "필터를 거짓 통과했다(활성 636종목이 어느 연도엔가 해당). 이제 eps·bps·sps가 동시에 0인 "
            "연도 레코드를 KIS 응답에서 걷어내고(drop_kis_placeholder_records — 스팩·자본잠식은 안 걸림), "
            "걷어낸 종목만 Naver 주요재무정보(최근 3개년 EPS/BPS/ROE/부채비율)로 보충한다(같은 연도는 "
            "KIS 우선). 걷어낸 연도는 '데이터 없음'이 되어 필터가 fail-closed로 제외하고, 직전 정상 "
            "연도가 있으면 다음 결산까지 forward-fill 규칙대로 이어진다. 기존 캐시·parquet은 "
            "scripts/repair_kis_placeholder_fundamentals.py로 수리(parquet의 자리표시자 서명 행을 비운 "
            "뒤 재구축 — 재구축은 캐시가 모르는 날의 기존 값을 보존해 먼저 비우지 않으면 옛 0이 남는다). "
            "PSR은 이 수정으로 복원되지 않는다(Naver 보충에 sps/매출이 없음).",
    "13.4.0": "복합 순위 합산 랭킹(FR-BT-063) 신설 — ranking_metric='composite'+ranking_components. "
            "구성 지표(재무 컬럼·기간 수익률·변동성)마다 전 지표가 정의된 종목 풀 안에서 횡단면 "
            "백분위 순위(방향 기준 좋은 쪽이 높게)를 매겨 동일 가중 평균 = 순위 합산 정렬. "
            "어느 한 지표라도 없는 종목은 후보 제외. opt-in 가산 — 단일 랭킹·비랭킹 전략 결과 불변.",
    "13.3.0": "**모멘텀(수익률) 랭킹 결과가 바뀐다 — 상장 전 backfill 오염 수정(13.2의 모멘텀 잔여분).** "
            "수익률 랭킹이 ffill+bfill된 price_df의 pct_change로 계산돼, 상장 전 구간이 첫 가격으로 "
            "뒤채워진 신규 상장 종목의 'N일 수익률'이 상장 이후 수익률로 위장됐다(2023-12-01 실측: "
            "상장 10거래일째 에코프로머티가 '최근 60거래일 수익률 상위 1%', 40거래일째 두산로보틱스가 "
            "상위 1%로 매수 — 60일 수익률이 정의될 수 없는 종목). 이제 bfill 전 raw_price_df에 ffill만 "
            "적용해 계산하고(engine.indicators.lookback_return_panel), 상장 후 lookback 봉이 쌓이기 "
            "전에는 NaN → 후보 자연 배제된다(라이브 신호 경로는 자기 봉 수 검사로 원래 안전). "
            "관측 미달 신규 상장이 편입되던 모멘텀 랭킹 전략은 매매·통계가 바뀐다.",
    "13.2.0": "**변동성 랭킹 결과가 바뀐다 — 상장 전 backfill 오염 수정.** 변동성 랭킹(13.1)이 "
            "ffill+bfill된 price_df로 수익률 표준편차를 계산해, 상장 전 구간이 첫 가격으로 "
            "평평하게 뒤채워진 신규 상장 종목의 변동성이 0에 가깝게 위장됐다(2022-07-01 실측: "
            "상장 21일째 마스턴프리미어리츠가 '120거래일 변동성 하위 7%'로 매수 — 120일 변동성이 "
            "정의될 수 없는 종목). 가짜 초저변동 종목들이 하위권을 채워 진짜 저변동 종목의 표시 "
            "백분위도 6~7%로 부풀려졌다(전 종목 재계산 대조: 실제 0.6~1.7%). 이제 bfill 전 "
            "raw_price_df에 ffill만 적용해 계산하고(engine.indicators.annualized_volatility_panel), "
            "수익률 관측치가 lookback개 미만이면 NaN → 후보 자연 배제된다. 변동성 랭킹을 쓰지 "
            "않는 전략의 결과는 불변. 대상 기능이 같은 날(13.1) 신설·미배포라 MAJOR 대신 패치로 "
            "남긴다. (모멘텀 'return' 랭킹도 같은 bfill 패널을 쓰는 기존 동작이 있으나 이 수정의 "
            "범위 밖 — 상장 후 실수익률로 계산되는 별개 특성이라 별도 판단 필요)",
    "13.1.0": "변동성 지표 신설(FR 미지원 승격) — ① technical 'volatility' 조건: N일(기본 60) "
            "일수익률 롤링 표준편차 × √246(KRX 실측, v12.0과 동일 계수) × 100(연환산 %)을 "
            "임계값 비교('변동성 30% 이하'), "
            "② ranking_metric='volatility': 같은 값의 횡단면 순위로 종목 선정(저변동성 랭킹, "
            "방향 미지정 기본 bottom — 무언의 top은 고변동성 선정으로 전략이 뒤집힌다). "
            "순수 가산 — 이 지표를 쓰지 않는 기존 전략의 결과는 불변이다.",
    "13.0.0": "**시가총액 실측 재구축 — market_cap·PCR·시총 필터/랭킹을 쓰는 전략의 결과가 "
            "바뀐다.** market_cap 컬럼이 '수정종가 × 현재 상장주식수' 근사여서(삼성전자 실측: "
            "2000~2026 전 구간 내재 주식수가 5.8463B로 상수) 증자·소각·우선주 등 주식수가 변한 "
            "종목의 과거 시총이 어긋났다 — 삼성전자 자사주 소각(2017~2018) 전 구간은 약 20% 과소, "
            "그만큼 PCR도 과소였다. KRX 일별 전 시장 스냅샷(pykrx get_market_cap_by_ticker, 당시 "
            "실제 상장주식수 기준)으로 전 종목 전 구간을 재구축했고(scripts/rebuild_market_cap.py), "
            "상폐 종목도 당시 스냅샷에 있으면 커버된다(PIT). PCR(=시총×1e8/영업CF)은 같은 기준으로 "
            "전면 재계산(recompute_pcr — enrich와 단일 정의 공유). 매일 sync는 날짜별 스냅샷 "
            "1콜로 새 봉에 실측 시총을 채우고, 실패·미커버(ETF 등)만 종전 근사(종가×현재 주식수) "
            "gap-fill로 폴백한다. 함께 고친 것: 시총 보강이 '값이 하나라도 있으면 통째로 스킵'이라 "
            "2026-06-26부터 새 봉의 market_cap→PCR이 영구 결측으로 쌓이던 사고를 결측 행만 채우는 "
            "gap-fill로 수정(재발 방지 회귀 테스트 포함). **매매·체결 자체가 아니라 유니버스 선별이 "
            "바뀐다** — 시총·PCR 조건/랭킹을 쓰지 않는 전략의 결과는 불변이다.",
    "12.0.0": "**연환산 기준 정정 — CAGR·샤프·소르티노·변동성 값이 모두 바뀐다.** 결과값 감사에서 "
            "찾은 지표 계산 오류를 한 번에 고쳤다. (1) *연수 세는 법*: CAGR이 연수를 '봉 수 ÷ 252'로 "
            "셌는데 KRX 실제 거래일은 연 246.5일(005930 기준 2010~2024)이라 분모가 커서 연수를 약 "
            "2% 적게 잡았고 그만큼 CAGR을 과대계상했다(실측 1477봉 구간: 5.861년 vs 실제 5.993년 → "
            "3.579% vs 3.499%). 이제 달력 경과일 ÷ 365.25로 센다. (2) *연환산 계수*: 샤프·소르티노·"
            "변동성의 √252를 KRX 실측 √246으로 바꾸고, 표준편차를 모집단(ddof=0)에서 표본(ddof=1)으로 "
            "바꿨다 — 종전은 변동성을 과소·샤프를 과대 평가했다. (3) *1년 미만 구간*: CAGR 자리에 "
            "연환산하지 않은 총수익률을 그대로 넣던 것을 정의대로 연환산하고(121봉 13.11% → 29.24%), "
            "짧은 표본의 연환산이 잡음을 증폭한다는 경고를 추가했다. (4) *손익비*: 손실 거래가 0건이면 "
            "vbt가 무한대를 주는데 safe()가 이를 0.0으로 뭉개 **전승한 전략이 손익비 0(최악)으로** "
            "표시됐다 — 정의되지 않음을 null로 내보낸다(표시는 ∞). (5) *켈리 기준*: 백엔드가 아예 "
            "계산하지 않아 프론트가 0으로 채웠고 AI 리포트 프롬프트에 '켈리 기준: 0.00%'가 사실처럼 "
            "주입됐다 — f* = W − (1−W)/R (R = 평균수익률÷평균손실률)로 신규 산출한다. (6) *종목별 "
            "CAGR*: vbt annualized_return(포트폴리오 초기자본 기준 + 365일 연환산)을 쓰는 바람에 같은 "
            "행의 totalReturn(종목 누적 진입원가 기준)과 분모가 어긋났다(005930 실측: totalReturn "
            "2.79%인데 cagr 2.86% — 이 cagr이 함의하는 총수익은 17.97%) — 이제 totalReturn을 같은 "
            "연수로 연환산한다. (7) *최대 연속패*: 손익 0인 거래를 패로 세던 것을 바로잡았다. "
            "**매매·체결·손익 자체는 전혀 바뀌지 않는다** — 달라지는 것은 그 위에 얹은 요약 통계다. "
            "저장된 과거 백테스트의 지표는 재현되지 않는다.",
    "11.0.0": "**벤치마크 오배정 수정 — buyAndHoldReturn·benchmark_label·benchmark_equity가 "
            "바뀐다.** 두 가지를 함께 고쳤다. (1) *오배정(값이 바뀌는 쪽)*: 벤치마크 선택이 "
            "universe_id의 시장 토큰만 봤기 때문에, 지정 종목·테마 유니버스처럼 universe_id가 "
            "None인 백테스트(strategy_converter가 target_symbols가 있으면 None으로 지운다)는 "
            "전부 기본값 KODEX 200과 비교됐다 — 코스닥 종목만 담긴 전략도 마찬가지였다. 이제 "
            "보유 심볼의 실제 시장을 다수결로 판정해(universe_pit.dominant_market) 벤치마크를 "
            "고른다. 또 코스피+코스닥 혼합 유니버스는 코스닥을 먼저 검사하는 순서 탓에 "
            "KODEX KOSDAQ 150과 비교됐는데, KODEX 코스피로 바로잡았고, 새로 배선된 "
            "kosdaq150 유니버스 토큰도 코스닥 계열로 인식시켰다(코스피200과 달리 별도 지수 "
            "ETF를 쓰지 않아 코스닥 전체와 같은 상품이 벤치마크다 — 모르면 KODEX 200으로 "
            "떨어졌다). 해당 백테스트들은 "
            "비교 대상 지수가 달라지므로 buyAndHoldReturn 값이 바뀐다. (2) *0% 뒤채우기 "
            "제거(곡선이 바뀌는 쪽)*: 벤치마크 정렬이 `.bfill()`로 지수 ETF 상장 이전 구간에 "
            "첫 가격을 뒤채웠다. 그 구간 수익률이 0%가 되지만 누적곱은 커버리지 구간만 곱한 "
            "것과 같아 **buyAndHoldReturn 값 자체는 종전과 동일하다** — 달라지는 건 "
            "benchmark_equity로, 존재하지도 않던 구간에 초기자본에서 평탄한 선이 그려지던 "
            "것을 null로 바꿨다. 다만 벤치마크가 자기 존재 구간만, 전략은 전체 구간을 복리로 "
            "쌓는 기간 불일치는 데이터로 메울 수 없어 경고와 신규 `benchmark_partial` "
            "플래그로 고지한다(표시 쪽이 초과수익률을 낼지 판단하는 근거). **어느 쪽도 전략 "
            "자체의 매매·손익은 바꾸지 않는다** — 달라지는 것은 비교 대상과 그 표시다.",
    "10.0.0": "**KIS 분기 행 혼입 제거 — 최근 구간의 재무 스크리닝 결과가 바뀐다.** KIS 재무 "
            "엔드포인트는 연간(FID_DIV_CLS_CODE=0)을 요청해도 최신 분기 한 행을 맨 앞에 끼워 "
            "보내는데(현대차 실측: stac_yymm 202603, 202512, 202412 …) 그동안 이를 연간 레코드로 "
            "취급해 왔다. 그 행의 비율은 연환산돼 정상이지만 **유량은 기중 누적**이라 1분기치다"
            "(현대차: ROE 89%·부채비율 101%인데 EPS 25%·영업이익 22%·EBITDA 23%·당기순이익 25%). "
            "그래서 결산일+90일 이후 구간에서 eps·ebit·ebitda·net_income이 약 1/4로, 성장률은 분기 "
            "행이 직전 '연간'과 비교돼 순이익증가율 -75%·영업이익증가율 -78%로, PER은 종가÷EPS라 "
            "약 4배로 왜곡됐다. 3,220종목 중 2,150종목(67%)의 최신 레코드가 분기였다. 이제 결산월"
            "(월 최빈값)과 다른 KIS 레코드를 병합 직후 한 자리에서 걸러낸다 — 3월·6월 결산 회사도 "
            "자동으로 맞는다. **과거 이력은 전부 연간이라 영향이 없고, 최근 구간(직전 결산 공시 "
            "이후)의 재무 필터·랭킹 통과 종목과 PER 기반 전략 결과가 달라진다** — 저장된 전략의 "
            "과거 백테스트 결과는 재현되지 않는다. 지표를 더한 게 아니라 잘못된 값을 걷어낸 "
            "것이라 MAJOR로 올린다.",
    "9.4.0": "분위(퀀타일) 그룹 비교·비율 선정(FR-BT-060/060b). 랭킹 후보를 종목 수 동일 G개 "
           "그룹으로 나눠 그룹별 백테스트를 반복 실행해 quantileGroups로 비교하고(메인 결과="
           "1그룹), '상위 X%' 비율 편입(max_positions_pct)과 그룹당 보유 상한"
           "(ranking_group_cap — 각 그룹이 구간 내 랭킹 상위 N종목만 보유)을 지원한다. "
           "새 파라미터가 없는 기존 전략은 선정 로직이 종전과 동일해 결과 불변(순수 가산).",
    "9.3.0": "지배주주순이익(owner_net_income, 억원)을 조건 필터·랭킹 지표로 추가. 기존 "
           "net_income은 KIS 순이익률x매출액이라 비지배지분이 섞인 **연결 전체** 당기순이익이고"
           "(삼성전자 2023: 154,843억 = 지배 144,734 + 비지배 10,137), 지배기업 소유주 귀속분은 "
           "DART 손익계산서의 별도 계정이다. 이미 종목·연도마다 부르던 fnlttSinglAcntAll 응답에서 "
           "같이 파싱하므로 추가 API 호출은 없다. DART 유래라 2015년 이전과 별도재무제표만 있는 "
           "종목은 결측이다. 순수 가산 — 이 지표를 쓰지 않는 기존 전략의 결과는 불변이다.",
    "9.2.0": "영업·투자·재무활동 현금흐름을 조건 필터 지표로 추가(operating_cf_amount/"
           "investing_cf_amount/financing_cf_amount, 억원). DART 원천은 raw 원이라 그대로 쓰면 "
           "'1,000억 이상'이 1억 배 어긋나므로, PCR·FCF 계산 기준인 raw 컬럼은 두고 억원 파생 "
           "컬럼을 신설해 그것만 필터에 노출한다. 투자·재무는 자산 취득·차입 상환으로 통상 음수라 "
           "부호를 보존한다. 순수 가산 — 이 지표를 쓰지 않는 기존 전략의 결과는 불변이다.",
    "9.1.0": "재무 팩터 랭킹('영업이익률 상위 20종목' 등, ranking_metric=재무 cid+top/bottom) 신설 "
           "+ 당기순이익(net_income, 억원)·PCR 지표 추가. 전부 opt-in 가산 — ranking_metric이 "
           "재무 지표일 때만 새 분기를 타고, 기존 전략(미지정·모멘텀 'return')의 결과는 불변이다. "
           "재무 랭킹 요청인데 유니버스에 지표 컬럼이 없으면 조용한 0거래 대신 경고를 남긴다.",
    "9.0.0": "백테스트 창의 **종료 경계가 배타적이던 off-by-one 수정** — 창 필터가 타임스탬프를 "
           "통째로 문자열화해 비교하는 바람에 `\"2024-12-30 00:00:00.000000\" <= \"2024-12-30\"`이 "
           "거짓이 되어, 명시 종료일 당일 봉이 매번 통째로 빠졌다(삼성전자 실측: endDate=2024-12-30 "
           "요청 시 마지막 봉이 2024-12-27). 시작 경계는 같은 규칙이 우연히 맞는 방향이라 끝에서만 "
           "하루가 사라지는 비대칭이었고, 종료일이 휴장일이면 증상이 가려져 오래 남았다. 이제 날짜 "
           "부분(YYYY-MM-DD)만으로 비교해 양끝을 포함한다(_date_key). 기간을 명시한 모든 백테스트는 "
           "마지막 거래일 하나가 더 포함되므로 수익률·MDD·거래 건수가 달라질 수 있다.",
    "8.0.0": "섹터 소속 판정을 사명 부분 문자열 매칭에서 KSIC 산업분류 코드 기반으로 재설계 "
           "(예: 메가스터디교육이 '가스'에 걸려 에너지/원자력으로 오분류되던 사고 등 96건 "
           "교정) + 섹터 소속 정본을 파일 캐시에서 지식그래프(company -belongs_to→ sector, "
           "3,242 엣지)로 전환 + 묶음 섹터 8쌍 분할(에너지/원자력 등 39→49). 업종/섹터로 "
           "필터하는 전략은 같은 입력이라도 유니버스에 포함되는 종목이 달라져 백테스트 "
           "결과가 바뀔 수 있다. 구 묶음 섹터명은 하위 호환 별칭(신규 두 섹터 합집합)으로 "
           "유지되어 저장된 전략·이력의 재현성은 보존된다.",
    "7.4.0": "신규 상장(IPO) 종목 유니버스(FR-STR-073) 추가 — 상장일이 지정 구간에 속하는 "
           "종목만 남기는 정적 심볼 필터(filter_by_listing_window). 섹터 필터와 같은 "
           "자리에서 같은 방식으로 적용되며, listing_from/listing_to를 지정하지 않는 "
           "기존 전략은 이 필터를 타지 않아 결과가 그대로 유지된다.",
    "7.3.0": "classify_universe가 정본 섹터명 밖 이질적 키워드 버킷 근사('태양광'→'에너지/원자력' 등, "
           "원자력·풍력·석유를 뭉뚱그림)를 더 구체적인 테마 카탈로그 존재 여부보다 먼저 확정하던 "
           "과대 유니버스 오분류를 구조적으로 수정(is_narrow_sector_approximation)",
    "7.2.0": "'인공지능/AI 관련주' 섹터 오버라이드 제거 — 업종 근사(소프트웨어/플랫폼 전체)로 "
           "유니버스가 과대해지던 오분류를 네이버 테마 카탈로그 종목 목록으로 대체",
    "7.1.0": "청산 사유 확정 통일(손절/익절/트레일링·리밸런싱 편출 라벨), 거래정지/수정주가 가드, "
           "배당 토탈리턴(opt-in), 단일·업종·ETF 유니버스, 생존편향 제거(PIT), 퀀트/펀더멘털 지표 확장",
    "7.0.0": "엔진 감사 대수술 — NAV 기준 사이징, 정수주 체결, 장중 스탑(same_close/next_open) 타이밍, "
           "Profit Factor raw 보고, 표준 Sortino/Sharpe(무위험수익률 반영)",
    "6.x": "모멘텀 랭킹 + 달력 리밸런싱(reconstitution), Walk-Forward·Monte Carlo 검증 정비",
    "5.x": "가상시장/워크포워드/몬테카를로 도입, Optuna 로컬 최적화",
    "4.x": "랭킹 기반 종목 선정(Value 50% + Quality 50%), AI 모델 연동",
    "3.x": "리스크 블록·최대 보유일·포지션 제약 정합성 수정, 비결정성 제거",
    "2.x": "엔진 모듈 분리 리팩터(Facade 패턴), 신호 격리",
    "1.x": "최초 백테스트 엔진 구현",
}
