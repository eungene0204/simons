fastapi==0.135.3
uvicorn==0.44.0
polars==1.39.3
pyarrow==23.0.1
pydantic==2.12.5
pandas==2.3.3
vectorbt==1.0.0
backtrader==1.9.78.123  # reference engine for cross-validation tests only
optuna==4.9.0
python-dotenv
requests==2.33.1
beautifulsoup4==4.14.3
websockets==16.0
langchain_openai==1.2.2
langchain_google_genai==4.2.4

# 엔진/AI 런타임 의존성 (main.py startup 체인에서 top-level import — 누락 시 기동 실패)
numpy==2.4.4
stockstats==0.6.8        # engine/indicators.py
pykrx==1.2.8             # engine/data_fetcher.py
httpx==0.28.1            # news providers
torch==2.12.0            # ai/ai_engine.py, ai/models.py — 앱박스는 GPU 없음. Dockerfile이 CPU 휠로 선설치(2.12.0+cpu)
xgboost==3.2.0           # ai/ai_engine.py
joblib==1.5.3            # ai/ai_engine.py 모델 로드
shap==0.51.0             # ai/xai_engine.py (XAI 설명)
chromadb==1.5.9          # advisor/vector_memory.py — 코치 RAG 메모리(Chroma 벡터스토어)

# NL 전략 생성 — Ollama 백엔드(배포 기본값) deps. engine/nl_parser.py _init_ollama가
# instructor.from_openai로 Ollama의 OpenAI 호환 엔드포인트(/v1) 호출. 없으면 전략 생성 비활성.
# (Mac dev는 MLX 백엔드라 outlines/mlx-lm 사용 — 그쪽은 이미지에 미포함)
openai==2.41.0
instructor==1.15.3
