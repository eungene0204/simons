# 파싱 정확도(배지)·미탐지 팩터 QA 리포트

- 합성 예시: **1000개** (기존 큐레이션 예시와 중복 없음)
- 배지 완전 일치(모든 팩터 정확): **992/992** (100.0%)
- 배지 불일치 예시: **0개** (파서 되물음으로 알림 0 · 조용히 누락 0)
- 파싱 실패(LLM 폴백 타임아웃): 8건

## 주요 발견 및 조치

초판(수정 전): 배지 완전일치 720/997 · 조용히 누락 277. 아래 조치 후 재실행: **배지 완전일치 992/992 · 조용히 누락 0**(파서가 결정론으로 처리하는 전 케이스에서 모든 팩터 정확). 결정론 슬롯(유니버스/펀더멘털/손절·익절·트레일링/포지션수/리밸런싱/연도범위/모멘텀 랭킹)은 값·연산자까지 정확.

**✅ 수정 완료**
1. **스토캐스틱·CCI 규칙 기반 미지원 → 추출기 추가** — 엔진·컨버터는 이미 지원했는데 NL 추출기만 없어 배지에서 조용히 누락되던 문제. `_extract_technical_signals`에 스토캐스틱/CCI(음수값 포함) 추출 추가(nl_parser.py) + 회귀 테스트.
2. **모멘텀 랭킹 + 명시적 보유기간 충돌 → 명시 보유기간 보존** — 'N일 수익률 상위 … 20일 보유 후 매도'에서 랭킹이 보유기간을 비우던 로직이 일 단위 명시 보유기간까지 삭제하던 문제. `_has_explicit_day_holding`로 모멘텀 룩백('3개월 오른')과 구분해 명시적 보유기간은 유지(nl_parser.py) + 양방향 회귀 테스트.
3. **트레일링 스탑 단독 청산 오차단 → 지원목록 보강** — 검증 agent가 합성 exit_rule의 `trailing_stop_pct`를 UNSUPPORTED_FIELD 에러로 차단(is_valid=false)하던 버그. `_SUPPORTED_CONDITIONS`에 `trailing_stop_pct` 추가(strategy_validation_agent.py) + 회귀 테스트.

**◻︎ 의도된 동작(미변경)**
4. **기술적 진입만 있고 청산이 없는 전략은 LLM 위임** — 예: 'MACD 골든크로스에 매수'. 청산 미기재는 모호한 입력이라 규칙 기반이 None을 반환하고 LLM에 위임하는 것이 **의도된 설계**(test_technical_entry_without_exit_still_falls_back). 이 환경에선 LLM 콜드스타트로 타임아웃 나 파싱 실패로 집계되지만, 파서 게이트를 바꾸면 이 설계를 깨므로 건드리지 않았다 — 별도 LLM 응답 견고화(타임아웃 시 graceful 폴백) 과제로 분리 권장.

## 팩터 종류별 미탐지/오값 (배지 정확도)

| 종류 | 정답 수 | 미탐지(MISSING) | 오값(WRONG_VALUE) | 정확도 |
|---|---|---|---|---|
| fund | 1795 | 0 | 0 | 100.0% |
| tech | 1240 | 0 | 0 | 100.0% |
| universe | 879 | 0 | 0 | 100.0% |
| maxpos | 800 | 0 | 0 | 100.0% |
| sl | 638 | 0 | 0 | 100.0% |
| tp | 552 | 0 | 0 | 100.0% |
| rebal | 302 | 0 | 0 | 100.0% |
| dates | 274 | 0 | 0 | 100.0% |
| trailing | 195 | 0 | 0 | 100.0% |
| ranking | 191 | 0 | 0 | 100.0% |
| hold | 145 | 0 | 0 | 100.0% |

## 의도적 미지원 팩터 검출 확인

스토캐스틱/CCI 등 규칙 기반 미지원 팩터를 일부러 251건 심었고, 하니스가 그 중 **0건**을 미탐지로 정확히 집어냈다.

## ⚠️ 조용히 누락된 빠진 팩터 (사용자 미고지)

정답에 있으나 파싱이 놓쳤고, 파서 되물음(clarification)도 없어 사용자가 누락을 알 수 없는 케이스. 배지에서 해당 팩터가 빠진다.

## 검증 agent 이슈 분포 (참고)

- `MISSING_TAKE_PROFIT`: 440건
- `MISSING_STOP_LOSS`: 354건
- `MISSING_EXIT_RULE`: 103건
