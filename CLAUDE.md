# Simons 프로젝트 Claude 지침

## 프로젝트 목표

이 서비스는 사용자가 자신만의 주식 투자 전략을 설계하고, 이를 체계적으로 검증할 수 있는 **종합 투자 시뮬레이션 플랫폼**이다.

사용자는 다양한 투자 지표와 자체 개발한 AI 예측 모델을 결합하여 고도화된 전략을 수립할 수 있다. 또한, 과거 데이터를 활용한 **백테스트(Backtest)** 는 물론, 실제 시장 상황을 반영한 가상 주식 시장에서의 **모의 매매**를 통해 전략의 유효성과 실전 수익성을 다각도로 실험할 수 있는 기능을 제공한다.

## 필수 규칙

### 코드 수정 후 유닛 테스트 전체 실행
코드를 수정할 때마다 반드시 모든 유닛 테스트를 실행하여 기존 기능이 깨지지 않았는지 확인한다.
- 백엔드 테스트: `cd backend && pytest tests/ --ignore=tests/test_backtest_engine.py --ignore=tests/test_engine_ai.py --ignore=tests/test_ai_sell.py --ignore=tests/test_api_isolation.py`
- 프론트엔드 테스트: `npm run test:frontend`
- 테스트 실패 시 반드시 수정 후 다시 통과시킬 것
- 사용자가 별도로 요청하지 않아도 자동으로 실행할 것

### 버그 수정 시 유닛 테스트 자동 추가
버그나 문제를 발견하고 코드를 수정하여 해결한 경우, 해당 버그를 재현하는 유닛 테스트를 자동으로 추가한다.
- 이미 동일한 케이스를 검증하는 테스트가 있으면 추가하지 않는다
- 백엔드 버그: `backend/tests/` 내 적절한 테스트 파일에 추가 (없으면 새 파일 생성)
- 프론트엔드 버그: `components/__tests__/` 또는 `tests/` 내 적절한 파일에 추가
- 사용자가 별도로 요청하지 않아도 자동으로 실행할 것

### 계획서 완료 표시 자동 업데이트
작업을 완료할 때마다 `docs/PROJECT_PLAN.md`의 해당 항목을 자동으로 완료 표시로 업데이트한다.
- 완료된 항목: `P1` / `P2` / `🔲 개발 필요` 등을 `✅ 완료`로 변경
- 사용자가 별도로 요청하지 않아도 자동으로 반영할 것

### UI 가이드라인 준수
새 페이지나 컴포넌트를 작성할 때는 반드시 `docs/UI_GUIDELINES.md`를 참고한다.
- 기준: 대시보드 페이지(`app/page.tsx`) 및 `components/dashboard/`에 적용된 스타일
- 색상, 타이포그래피, 간격, 카드 스타일, 레이아웃 패턴 등 가이드라인에 명시된 값을 준수할 것
- 기존 페이지 수정 시에도 가이드라인과 불일치하는 부분이 있으면 맞춰서 수정할 것
