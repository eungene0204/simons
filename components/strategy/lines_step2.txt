              {/* Navigation Buttons */}
              <div className="absolute bottom-8 right-8 flex items-center gap-2 z-50">
                <button
                  onClick={() => setCurrentStep(1)}
                  className="px-5 py-2.5 bg-[#1a1a1a] border border-gray-700 text-white rounded-lg text-sm hover:bg-gray-800 flex items-center gap-2 shadow-lg transition-all hover:scale-105"
                >
                  <ArrowLeftIcon className="w-4 h-4" />
                  이전: 유니버스
                </button>
                <button
                  onClick={() => setCurrentStep(3)}
                  className="px-5 py-2.5 bg-blue-600 text-white rounded-lg text-sm font-bold hover:bg-blue-500 flex items-center gap-2 shadow-lg shadow-blue-900/20 transition-all hover:scale-105"
                >
                  다음: 포지션/비중
                  <ArrowRightIcon className="w-4 h-4" />
                </button>
              </div>
