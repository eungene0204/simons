# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# Claude Code local-only files (project hook config in .claude/settings.json IS tracked)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
/prisma/dev.db
/prisma/dev.db-journal
/prisma/live_backup.db
/prisma/*.db-shm
/prisma/*.db-wal
prisma/prisma/dev.db
prisma/prisma/dev.db-shm
prisma/prisma/dev.db-wal

# redis
dump.rdb

# python
__pycache__/
*.py[cod]
*$py.class
.venv
venv/
ENV/

# data
/data/ohlcv/*.parquet
/data/ohlcv/*.json
/data/ohlcv-us/*.parquet
/data/ohlcv-us/.last_full_refresh
/data/ohlcv-us/.edgar_fundamentals_done
/data/index/*.parquet

# 수정 요청 학습 코퍼스 (런타임 누적, bind-mount 영속)
/data/modify_corpus/

# Logs
/logs/
/backend/logs/
*.log
dev-debug.log
# Dependency directories
node_modules/
# Environment variables
# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Fundamental data cache
/data/fundamentals/

# OS specific

# Task files
# tasks.json
# ChromaDB indices
backend/advisor/.chroma/

# 코퍼스 빌드 데이터 산출물(스크립트 재생성 — build_strategy_corpus.py)
backend/advisor/extracted_dsls.jsonl
backend/advisor/corpus_backtests.jsonl

# Advisor smoke test data (auto-generated, large files)
data/advisor-learning/advisor_smoke_*_results.json
data/advisor-learning/advisor_smoke_*_batch_payload.json
data/advisor-learning/advisor_smoke_*_samples.jsonl
data/advisor-learning/advisor_smoke_extra_*
data/advisor-learning/advisor_paired_smoke_results.json
data/advisor-learning/advisor_paired_smoke_batch_payload.json
data/advisor-learning/advisor_paired_smoke_samples.jsonl
data/advisor-learning/strategy_prompt_experiment_result.json

# Model backups and candidates
model/v3_candidate/
model/v3_pre_promote_*
model/v3_pre_resync_*
model/v3/model_meta.pre_recalib_*
top200.txt

# KRX 일별 스윕 캐시(backfill_delisted_etf.py 재개용 — 재생성 가능)
/data/cache/

# 용어 그라운딩 어휘집(런타임 학습 산출물 — 환경별로 자라남)
/data/term_lexicon.json
/data/us-term-lexicon.json
/data/kg-equity-edges.progress.json
/data/cashflow-activity-backfill.progress.json
/data/owner-net-income-backfill.progress.json
/data/owner-net-income-backfill.progress.v2.json

# DART 기업 고유번호 캐시 — 재다운로드 가능(backfill_dart_industry.py)
data/dart-corp-code.json

# QA 하니스 로컬 캐시(기준 전략·실행 결과)
scripts/.qa_free_input_*.json

# 예시 QA 하니스 생성물 — 로컬에만 둔다(scripts/qa_template_detect.py).
# 캐시는 1.7MB 파생물이라 스윕마다 저장소를 흔들고, 리포트는 추적하면 **낡은 채로 남아**
# 오히려 오판을 부른다(2026-08-14 사고: 5일 묵은 리포트의 정상 값을 보고 결함을 놓쳤다,
# FR-STR-019x ②). 판정 결과는 커밋 메시지·SRS·PROJECT_PLAN에 서술로 남긴다.
scripts/.template_parse_cache.json
docs/template_detect_report.md

# 전단지·소셜 이미지 렌더 산출물 — 원본 HTML(docs/marketing/*.html)과 마크·QR만 추적한다.
# PNG는 헤드리스 Chrome로 언제든 다시 뽑히는 파생물이고 한 장에 0.5~1MB라
# 문구를 손볼 때마다 저장소를 흔든다(2026-09-14 사용자 결정).
docs/marketing/nullstock_*.png

# 게스트(테스터) 계정 발급 기록 — 비밀번호 원문 포함, 추적 금지 (scripts/create-guest-accounts.ts)
/guest-accounts*.local.json
